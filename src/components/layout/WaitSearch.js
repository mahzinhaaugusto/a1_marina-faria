import { Center, Text } from "native-base";

export const WaitSearch = () => {
    return (
        <Center margin={50}>
            <Text fontSize={22} fontWeight="bold" textAlign="center">Please initiate a search</Text>
        </Center>
    )
}